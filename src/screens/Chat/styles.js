import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
    padding: 16,
    backgroundColor: '#fff',
  },
  header: {
    fontSize: 30,
    fontWeight: 'bold',
    marginBottom: 16,
    color: '#333',
    paddingTop: 25,
  },
  conversationItem: {
    padding: 16,
    borderBottomWidth: 1,
    borderColor: '#ccc',
  },
  conversationText: {
    fontSize: 16,
  },
});

export default styles;
